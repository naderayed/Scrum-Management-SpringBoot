package com.nader.scrum.management.repositories;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class AppUserRepoTest {

    @Test
    void findByFirstnameAndLastname() {

    }
}

/*
DTO stand for Data Transfer object:
Limit the attributes that will be exposed to the client
on another word the client does not know the real data inside our api only he can access to the exposed ones .
With DTO we can build different views with our domain models.
reduce the numbers of calls.
Mapper: that will perform the data transform data type to another .

*
* */